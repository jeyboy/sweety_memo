.row
  .col-xs-12
    %h1
      = t('token.editing', scope: object.class.name.underscore)
      = link_to t('panel.views.show'), [:panel, object], class: 'btn btn-info'

= render "panel/#{object.class.name.underscore.pluralize}/form"

