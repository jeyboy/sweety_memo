%h1 Listing Posts

- if @posts.present?
  %table.table.table-striped
    = render partial: 'panel/posts/post', collection: @posts
- else
  .text-center
    %h1 No posts yet
    = link_to 'Add post', new_panel_post_path, class: 'btn btn-success'
