.title_with_margin
  .inlined= image_tag((@category.image.file.thumbnail.url rescue ImageUploader::default_url), height: '100', class: 'img-rounded')
  %h1.inlined= t('token.gallery_items')

.row
  = render @gallery_items

.col-xs-12
  = paginate @gallery_items, theme: 'twitter-bootstrap-3'
